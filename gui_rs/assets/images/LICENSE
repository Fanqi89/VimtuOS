gui_rs/assets/images —— 许可（License）
========================================

本目录下的图片（wallpapers/、avatars/）都是**本仓库自绘**的，不是第三方素材：

* 生成脚本：`tools/make_asset_images.py`（纯 Pillow 绘制，确定性输出）
* 版权与许可：**CC0-1.0**（公众领域贡献）—— 本仓库（VimtuOS 项目）将其自绘作品按 CC0-1.0 释出，
  可自由复制、修改、分发，无需署名。来源 URL：无（本仓库原创）。
* 不含任何商标、品牌 logo 或来源不明的"免费素材包"内容。

说明（真源口径，避免误读）：
* 桌面**壁纸**的运行期真源仍是内核程序化壁纸（`kernel/gfx64.cpp` 的
  `gfx64_wall_build_default64`）与用户配置的 VimtuFS2 壁纸文件（`ui.wall.path`）；
  本目录的 PNG 是 assets 结构下的自绘样例素材。
* **头像**的运行期真源仍是 `kernel/userdb64.cpp` + 开始菜单的程序化头像（首字母 + 主题渐变圆）。
