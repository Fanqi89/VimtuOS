# Vimtu64 构建与运行说明（纯 64 位）

> 本项目**只有 64 位一条构建线**：内核、loader、安装程序全部是 x86_64 长模式代码。
> 唯一构建脚本是 **`build64.sh`**。原先的 32 位工程已整体退役到 `legacy32/`，
> 不参与构建、也不被 64 位代码引用（`legacy32/README.md`）。

## 开工前必做：先核查状态

```bash
python tests/status_report.py          # 静态核查（秒级，带证据）：哪些已完成/部分/未做
python tests/status_report.py --full   # 再真跑一遍全部验收测试
```

它把每项能力绑定到可核对的证据（源文件、关键符号、产物字节、测试脚本），输出
DONE/PARTIAL/MISSING 三态。**不要凭记忆判断进度**——结论看
`docs/项目状态总览.md`（与脚本同一套判据）。当前基线：完成 14 / 部分 1 / 未做 9。
---

## 0. 目录速览

```
build64.sh                 唯一构建脚本（64 位）
boot/                      引导层（16 位实模式 + 长模式入口，物理必经）
  boot.asm                 512B MBR（读 LBA1..8 到 0x9000）
  loader64.asm             loader64：E820/VBE → ATA PIO 读内核 → 进长模式
  loader64_atapi.inc       ATAPI(PACKET) 读光盘（%include 进 loader64）
  cdiso.asm                安装介质的引导桩（光盘 El Torito / U 盘硬盘分支共用）
  hybrid_mbr.asm           写进 ISO 第 0 扇区的 hybrid MBR（U 盘引导）
kernel/
  entry64.asm              长模式入口（设置栈 → 跳 kmain64）
  kernel64.cpp             64 位内核入口 / 自检 / 安装程序或系统两条路径
  x86_64.cpp               IDT / TSS / PIC / PIT / RTC（64 位）
  fb.cpp font.cpp input.cpp 图形与输入（与语言无关的纯 IO 层）
  mem64.cpp                64 位内存管理：物理页池 + 内核堆 + 归属记账 + 编译器辅助例程
  gui64.cpp                64 位桌面外壳：窗口管理/z 序/拖拽/任务栏/开始菜单/脏矩形重绘
  calc64.cpp mines64.cpp   计算器 / 扫雷（各自独立 .cpp，走 gui64.h 契约）
  terminal64.cpp           终端 + 内建 Shell（30+ 命令 + 最小 ramfs）
  settings64.cpp           设置页（分辨率/缩放/语言真能改；刷新率如实标注未支持）
  taskmgr64.cpp            Win10 风格任务管理器（进程/性能/启动/详细信息）
  gui64.h                  桌面外壳与应用之间的**接口契约**（新增应用照它写）
  ata64.cpp part64.cpp     存储：ATA PIO + ATAPI(PACKET)；分区表/安装引擎
  setup64.cpp              Win10 风格安装向导（语言→现在安装→许可→类型→分区→进度→完成）
  isr_stubs64.asm switch64.asm linker64.ld
tools/make_iso64.py        打包 ISO + 按魔数回填 LBA + 写入 hybrid MBR
tests/                     全部验收脚本（见第 4 节）
legacy32/                  已退役的 32 位工程（可整目录删除）
```

## 1. 构建前置（Windows + MSYS2）

```bash
pacman -S --noconfirm --needed mingw-w64-x86_64-clang mingw-w64-x86_64-lld \
                               mingw-w64-x86_64-compiler-rt nasm xorriso
```

还需要 Python 3 + fontTools + Pillow（字体子集化/图标生成）：`pip install fonttools pillow`。QEMU 用于自动化验收
（`C:\Program Files\qemu\qemu-system-x86_64.exe`）。

字体源文件**不进仓**：构建前先按 `docs/字体许可说明.md` 下载三套开源字体（Noto Sans / Noto Serif / Noto Sans SC，SIL OFL 1.1）
到 `Fonts-open/`（含三份 OFL 许可全文）。`Fonts/`（Windows 字体副本）已不再被构建使用。


> **UEFI（双模式）现状**：ISO 里已经带 El Torito 的 UEFI 引导项（platform 0xEF）+ GPT + FAT16 的
> ESP，`BOOTX64.EFI`（自研 PE32+ 桩）→ `UEFI64.BIN`（平铺长模式引导器）两段式引导程序已完成，
> 固件能挂载 ESP、能加载桩（实测 UEFI Shell 里 `load` 成功）；**但固件"自动引导"这一步还没打通**。
> 详见 `docs/UEFI引导说明.md`（含已验证项、排查过的因素与下一步方向）。
## 2. 构建

```bash
bash build64.sh
```

产出（全部在仓库根目录 / `build64/`）：

| 文件 | 说明 |
|---|---|
| `vimtu64-64.iso` | **安装介质 ISO**（BIOS 光盘引导 + hybrid MBR，U 盘可写） |
| `vimtu64-64.img` | 安装介质裸盘镜像（硬盘引导路径用） |
| `build64/kernel64.bin` | 安装程序内核（带 `-DVIMTU_INSTALLER_MEDIA`） |
| `build64/kernel64_os.bin` | **装进硬盘的系统内核** |
| `build64/system.img` | 系统镜像载荷（8073 × 512B，整盘内容） |
| `build64/{boot,loader64,cdiso,hybrid_mbr}.bin` | 引导层各段 |

镜像布局（三方一致：`kernel/memlayout64.h` / `boot/loader64.asm` / `build64.sh`）：

```
LBA 0            : boot.bin（512B MBR）
LBA 1..8         : loader64.bin
LBA 9..8008      : kernel64*.bin（最多 4MB）
LBA 8009..8072   : 设置持久化保留区（64 扇区）
---- 以上 8073 扇区 = 一份完整系统磁盘映像（system.img）----
LBA 8192         : 载荷头（magic "VIMTUPAY" + 扇区数 + 载荷 LBA）
LBA 8193..16265  : 载荷本体（system.img 的副本）
```

## 3. 三种引导路径（都已实测）

| 路径 | 过程 |
|---|---|
| **光盘**（`vimtu64-64.iso` 挂光驱） | El Torito → `cdiso.asm`（kind=1）→ loader64 用 **ATAPI** 读内核 → 长模式 |
| **U 盘**（ISO 写进 U 盘 / 当硬盘挂） | ISO 第 0 扇区 hybrid MBR → `cdiso.asm` 硬盘分支 → INT 13h 分块读 + 每块一次保护模式搬进高内存（`0x100000` / `0x04000000`）→ **kind=2（RAM 源）** → loader64 |
| **裸盘介质**（`vimtu64-64.img` 当硬盘） | MBR → loader64 用 ATA PIO 读内核（无描述符） |

安装程序读到介质描述符后：kind=1 走 ATAPI 读载荷，kind=2 直接用内存里的载荷；
两者都会**识别出介质盘自己**（ISO9660 主卷描述符 `CD001` 指纹）并从向导默认选中行里跳过。

## 4. 验收（全部应 PASS）

```bash
python tests/boot64_assert.py                       # M0/M1：长模式/IDT/PIT/BootInfo
python tests/mouse_parse_test.py                    # PS/2 鼠标解析 + 累加器（VMware 实抓数据复放）
python tests/install_flow_test.py                   # 裸盘介质端到端安装（字节级）
python tests/partition_ops_test.py                  # 新建/格式化/删除 真实写盘
python tests/iso64_install_test.py                  # ISO 光盘引导端到端安装
python tests/iso64_usb_test.py                      # U 盘/hybrid 引导端到端安装
python tests/iso64_usb_test.py --mode usb           # USB 存储形态（真 U 盘总线）
python tests/screen64_probe.py                      # 安装界面渲染（分辨率/配色/按钮）
set VIMTU_VM_ISO=1 && python tests/vmware_install_test.py   # VMware 真 BIOS/PS2/光驱全流程
python tests/desktop64_test.py                      # **64 位桌面栈**：外壳+8 个应用+像素+脏矩形
python tests/screenshot64.py                        # 抓一张桌面真机截图（PNG，存 docs/screenshots/）
```

手工试跑：

```bash
# QEMU：光盘引导
qemu-system-x86_64 -cdrom vimtu64-64.iso -drive format=raw,file=target.img -m 512 -vga std

# QEMU：把 ISO 当硬盘（等价于"dd 进 U 盘"）
qemu-system-x86_64 -drive format=raw,file=vimtu64-64.iso,index=0,media=disk \
                   -drive format=raw,file=target.img,index=1,media=disk -m 512 -vga std

# VMware：新建 VM 时 guestOS 必须选 64 位（other-64），否则 CPUID 长模式位被屏蔽
```

## 5. 安装流程（与 Win10 一致，去掉密钥步骤）

1. 语言与区域 → 2. 现在安装 → 3. 许可条款 → 4. 安装类型（升级/自定义）
→ 5. 磁盘与分区（**新建 / 删除 / 格式化**、选择分区） → 6. 复制与进度（真实百分比）
→ 7. 完成并**自动重启**。

装完后硬盘布局：`P1 = 0xEF 活动引导分区（LBA 9 + 8000）`、`P2 = 0x07 主分区（8009..盘尾）`，
`P1` 内即 loader64 + 系统内核，整盘可直接引导进长模式。

## 6. 版本约定

* 引导层里出现的 16 位实模式代码**只在物理必经阶段**（BIOS 中断、进出长模式），
  没有任何 32 位业务代码；UEFI 段（自研 PE/EFI 头 + GPT/FAT32 ESP）做完后，
  引导链也可以做到 100% 64 位。
* 不要修改 `KERNEL_SECTORS`（4MB）等布局常量而不改三处（`memlayout64.h`、
  `boot/loader64.asm`、`build64.sh`）；`build64.sh` 末尾有超限检查会直接报错。
